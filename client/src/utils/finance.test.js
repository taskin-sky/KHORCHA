import { describe, expect, it } from 'vitest'; import { reconcile, shiftMonth, statusFor, syncCategoryBudgets } from './finance';
describe('finance helpers',()=>{it('reconciles the sample plan exactly',()=>{const r=reconcile({income:35000,saving:5000,budgets:[{amount:4500},{amount:14500},{amount:9000},{amount:2000}]});expect(r).toEqual({planned:35000,unallocated:0,over:0})});it('flags over-budget spending',()=>{expect(statusFor(120,100)).toBe('Over budget')});it('does not produce NaN without a budget',()=>{expect(statusFor(50,0)).toBe('No budget set')});it('moves December plan to next January',()=>{expect(shiftMonth(2026,12,1)).toEqual({year:2027,month:1})});it('moves January plan to previous December',()=>{expect(shiftMonth(2026,1,-1)).toEqual({year:2025,month:12})});it('removes inactive plan categories and adds new active categories',()=>{const result=syncCategoryBudgets([{categoryId:'old',plannedAmount:9500}],[{_id:'old',name:'Sublet + Khabar',type:'expense',isActive:false},{_id:'new',name:'Basha Vara',type:'expense',isActive:true}]);expect(result).toEqual([{categoryId:'new',label:'Basha Vara',plannedAmount:0}])})});
