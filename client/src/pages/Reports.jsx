import { useQuery } from '@tanstack/react-query'; import { Download } from 'lucide-react'; import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'; import { api } from '../services/api'; import { money } from '../utils/finance'; import { Card, PageHead, Spinner } from '../components/UI';
export default function Reports(){const{data,isLoading}=useQuery({queryKey:['reports'],queryFn:()=>api.get('/reports/daily')});if(isLoading)return <Spinner/>;const d=data.data;const download=async()=>{const r=await fetch(`${import.meta.env.VITE_API_BASE_URL||'http://localhost:5000/api'}/reports/export`,{headers:{Authorization:`Bearer ${localStorage.getItem('khorocha-token')||sessionStorage.getItem('khorocha-token')}`}});const a=document.createElement('a');a.href=URL.createObjectURL(await r.blob());a.download='khorocha-report.csv';a.click();URL.revokeObjectURL(a.href)};return <div className="page"><PageHead eyebrow="MONEY INSIGHTS" title="Reports" action={<button className="btn secondary" onClick={download}><Download/>Export CSV</button>}/><section className="plan-summary"><Card><small>Income</small><strong>{money(d.summary.income)}</strong></Card><Card><small>Expense</small><strong>{money(d.summary.expense)}</strong></Card><Card><small>Saved</small><strong>{money(d.summary.saving)}</strong></Card></section><Card className="chart-card"><div className="card-title"><div><p className="eyebrow">DAILY TREND</p><h2>Spending this month</h2></div></div><ResponsiveContainer width="100%" height={300}><BarChart data={d.rows}><CartesianGrid vertical={false} stroke="var(--line)"/><XAxis dataKey="_id"/><YAxis hide/><Tooltip formatter={v=>money(v)}/><Bar dataKey="amount" fill="#5177ff" radius={[8,8,0,0]}/></BarChart></ResponsiveContainer><p className="sr-summary">This chart shows daily expenses for the selected month, totalling {money(d.summary.expense)}.</p></Card></div>}
